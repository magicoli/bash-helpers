for FILE in `cat ~/.filesuck2.files `
do
	FILENAME=`basename $FILE`
	RESULT=`find ~/Motivation/kiss/ -name $FILENAME`
	if [ ! "$RESULT" ]
	then
		echo $FILENAME
		wget -nd -P ~/Motivation/kiss $FILE
	fi
done
